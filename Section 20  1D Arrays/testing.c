#include <stdio.h>

#define SIZE 21
int main(){ 
    int i, game;
    int array[SIZE]= {};

    game, array[0] = 5; // notice that you cannot define something like this. 
    printf("%d\n", game);
    printf("%d", array[0]);


    // for (i = 0; i < SIZE; i++){
    //     printf("%c ", array[i]);
    // }
    // printf("\n");
    // array[3] = '3';
    // for (i = 0; i < SIZE; i++){
    //     printf("%c ", array[i]);
    // }

    return 0;
}